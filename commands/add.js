module.exports = {
    name: 'add',
    category: 'add',
    description: 'add uid',
    usage: `add`,
    async execute(message, args, command, client, Discord, db){
        //check arguments
        var uid = args[0];
        


    }
}
